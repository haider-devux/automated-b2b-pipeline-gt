# Granjur B2B Pipeline — Python dependencies (exact versions in use)
# Install:  python -m pip install -r requirements.txt
# Tested on Python 3.13

# --- Core / directly imported by the project ---
Flask==3.1.3            # dashboard + tracking/webhook servers
psycopg2-binary==2.9.12 # PostgreSQL driver
requests==2.34.2        # HTTP (OpenStreetMap/Overpass, site scraping, Ollama, PageSpeed)
dnspython==2.8.0        # MX-record checks before trusting/guessing an email
python-dotenv==1.2.2    # load .env config
openpyxl==3.1.5         # Excel (.xlsx) reports — snapshots + central database
python-docx==1.2.0      # generates the features Word document
tzdata==2026.3          # IANA timezones for send-window scheduling (required on Windows)

# --- Optional ---
# pandas>=2.0           # faster bulk registry/quota CSV import (scripts/import_public_registry.py,
#                       # scripts/get_quota_leads.py). If absent, the code falls back to the stdlib csv module.
# playwright>=1.44      # REQUIRED ONLY for bot-gmaps (Google Maps scraper, wf1_python/collect_maps.py).
#                       # After install:  playwright install chromium
#                       # Also set GMAPS_PROXIES (rotating residential proxies) — see DEPLOYMENT_PLAN.md §4.1.
#                       # None of the other bots need it; leave commented if you don't run bot-gmaps.

# --- Pulled in automatically as sub-dependencies of the above (listed for a fully pinned lock) ---
Jinja2==3.1.6
Werkzeug==3.1.8
MarkupSafe==3.0.3
itsdangerous==2.2.0
click==8.4.2
blinker==1.9.0
colorama==0.4.6
lxml==6.1.1
et_xmlfile==2.0.0
certifi==2026.6.17
charset-normalizer==3.4.8
idna==3.18
urllib3==2.7.0
typing_extensions==4.16.0
